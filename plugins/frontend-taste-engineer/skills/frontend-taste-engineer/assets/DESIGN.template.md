# Design lock

## Operating mode and evidence

- Operating mode:
- Build mode (new build or redesign):
- Supplied facts and quoted text:
- Product assumptions:
- Clarification asked or judgment defaults used:

## Product and user

- Product type:
- Primary user and job:
- Trust/risk level:
- Device and environment:
- Known constraints:
- Assumptions to verify:

## Retrieved evidence before lock

- Core UX record IDs:
- Source-derived design record IDs and source families:
- Copy record IDs:
- Accessibility, responsive, integrity, and verification record IDs:
- License and allowed-use restrictions:

## Candidate directions

### Candidate A

- Composition / typography / identity mechanism:
- Color / surfaces / geometry / media:
- Motion posture / responsive behavior / content shape:
- Evidence IDs and tradeoffs:

### Candidate B

- Composition / typography / identity mechanism:
- Color / surfaces / geometry / media:
- Motion posture / responsive behavior / content shape:
- Evidence IDs and tradeoffs:

### Candidate C (optional)

- Material difference and evidence:

## Selection and design thesis

- Selected direction and why:
- Rejected directions and why:

One sentence that defines product character and the intended hierarchy. Must reject plausible alternatives (not “modern and clean”).

## Why this is not generic

One sentence proving the page could not be confused with a default AI landing after removing the logo.

## Direction

- Subject vocabulary (real materials, tools, language, environment, evidence; selected and rejected signals):
- Density profile (`sparse-editorial` · `marketing-landing` · `product-marketing` · `dense-app` · `data-dashboard` · `portfolio-expressive`):
- Composition (first viewport: brand, one headline, one support line, one CTA group, one visual plane):
- Spacing scale / section rhythm:
- Typography (display + body pair; explicit scale; no Inter/Roboto/Arial identity):
- Color roles (bg / surface / text / mute / accent):
- Material / surface language:
- Imagery/iconography:
- Motion intensity + at most three roles (focal · state · feedback) and reduced-motion outcomes:
- Familiarity vs. originality:
- Structural signals (what numbering, labels, dividers, or grouping actually encode):
- Action vocabulary (core verbs/nouns from trigger through result and recovery):
- Patterns intentionally avoided (reject list):

## Selected system lock (after retrieval and comparison)

Confirm the fields above are filled after diversified source, copy, UX, and safety retrieval plus candidate comparison.

- Existing system to preserve or extend:
- Tokens and component strategy:
- Responsive strategy:
- Accessibility target:
- Performance budget:

## States and acceptance criteria

List required loading, empty, error, success, permission, offline, destructive, and recovery states plus measurable completion criteria.

## Source usage ledger

- Constraint- or thesis-derived queries used:
- Sources consulted:
- Applied record/source IDs:
- Rejected record/source IDs and reason:
- What was adapted vs ignored:

## Rendered refinement (mandatory)

- Desktop/mobile captures (paths):
- Three highest-impact weaknesses:
- Fixes implemented:
- Revised captures:
- Second pass needed? (if still generic / fails brand test):
- Remaining limitations:
