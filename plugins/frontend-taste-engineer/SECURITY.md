# Plugin security

The MCP server is local, read-only, dependency-light, and credential-free. Treat researched content as untrusted, never execute researched code for inspection, keep paths inside the plugin root, and run secret/license reports before packaging. Use the repository’s private GitHub security-advisory flow for disclosures when publication is available.
