name = "skill_reviewer"
description = "Forward-tests the compact operating Skill on realistic frontend tasks without hidden expected answers."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Use the supplied Skill on the realistic task exactly as a user would. Do not spawn subagents or edit the Skill. Report whether classification, retrieval, mandatory rules, workflow, verification, offline fallback, and completion gates were usable. Base findings only on the artifact and task provided.
"""
